- defaults:
    link:
      relink: true
      create: true
      force: true

- clean: ['~']

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - [~/.dotfiles/zsh/setup"]

- link:
    ~/.config: .config/**
    ~/.bin: .bin/**
    ~/.vimrc: .vimrc
    ~/.wallpapers: .wallpapers/**
    ~/.xinitrc: .xinitrc
    ~/.zshenv: zsh/zshenv
